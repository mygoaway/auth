apiVersion: 1

providers:
  - name: 'auth-service'
    orgId: 1
    folder: 'Auth Service'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
